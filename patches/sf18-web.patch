diff --git a/src/engine.cpp b/src/engine.cpp
index dfd2099..26de6e0 100644
--- a/src/engine.cpp
+++ b/src/engine.cpp
@@ -150,2 +150,4 @@ Engine::Engine(std::optional<std::string> path) :
-    load_networks();
+#if !defined(__EMSCRIPTEN__)
+    load_networks();
+#endif
     resize_threads();
@@ -317,6 +317,18 @@ void Engine::load_small_network(const std::string& file) {
     threads.ensure_network_replicated();
 }
 
+void Engine::load_big_network(std::istream& is) {
+    networks.modify_and_replicate([&is](NN::Networks& networks_) { networks_.big.load(is); });
+    threads.clear();
+    threads.ensure_network_replicated();
+}
+
+void Engine::load_small_network(std::istream& is) {
+    networks.modify_and_replicate([&is](NN::Networks& networks_) { networks_.small.load(is); });
+    threads.clear();
+    threads.ensure_network_replicated();
+}
+
 void Engine::save_network(const std::pair<std::optional<std::string>, std::string> files[2]) {
     networks.modify_and_replicate([&files](NN::Networks& networks_) {
         networks_.big.save(files[0].first);
diff --git a/src/engine.h b/src/engine.h
index 10c92d7..4d933b9 100644
--- a/src/engine.h
+++ b/src/engine.h
@@ -22,6 +22,7 @@
 #include <cstddef>
 #include <cstdint>
 #include <functional>
+#include <iosfwd>
 #include <map>
 #include <optional>
 #include <string>
@@ -89,6 +90,8 @@ class Engine {
     void load_networks();
     void load_big_network(const std::string& file);
     void load_small_network(const std::string& file);
+    void load_big_network(std::istream& is);
+    void load_small_network(std::istream& is);
     void save_network(const std::pair<std::optional<std::string>, std::string> files[2]);
 
     // utility functions
diff --git a/src/main.cpp b/src/main.cpp
index 9a7376e..139391d 100644
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -27,17 +27,23 @@
 
 using namespace Stockfish;
 
+UCIEngine* uci_global;
+
 int main(int argc, char* argv[]) {
     std::cout << engine_info() << std::endl;
 
     Bitboards::init();
     Position::init();
 
-    auto uci = std::make_unique<UCIEngine>(argc, argv);
+    char* browserArgv[] = {const_cast<char*>("stockfish"), nullptr};
+    uci_global          = new UCIEngine(1, browserArgv);
 
-    Tune::init(uci->engine_options());
+    Tune::init(uci_global->engine_options());
 
-    uci->loop();
+    uci_global->loop();
+
+    delete uci_global;
+    uci_global = nullptr;
 
     return 0;
 }
diff --git a/src/memory.cpp b/src/memory.cpp
index 94a5993..dadfcb9 100644
--- a/src/memory.cpp
+++ b/src/memory.cpp
@@ -135,7 +135,7 @@ void* aligned_large_pages_alloc(size_t allocSize) {
     // Round up to multiples of alignment
     size_t size = ((allocSize + alignment - 1) / alignment) * alignment;
     void*  mem  = std_aligned_alloc(alignment, size);
-    #if defined(MADV_HUGEPAGE)
+    #if defined(MADV_HUGEPAGE) && !defined(__EMSCRIPTEN__)
     madvise(mem, size, MADV_HUGEPAGE);
     #endif
     return mem;
@@ -159,7 +159,7 @@ bool has_large_pages() {
         return true;
     }
 
-#elif defined(__linux__)
+#elif defined(__linux__) && !defined(__EMSCRIPTEN__)
 
     #if defined(MADV_HUGEPAGE)
     return true;
diff --git a/src/misc.cpp b/src/misc.cpp
index 5f8f31a..d491602 100644
--- a/src/misc.cpp
+++ b/src/misc.cpp
@@ -480,6 +480,8 @@ bool is_whitespace(std::string_view s) {
 }
 
 std::string CommandLine::get_binary_directory(std::string argv0) {
+    return {};
+
     std::string pathSeparator;
 
 #ifdef _WIN32
@@ -514,6 +516,8 @@ std::string CommandLine::get_binary_directory(std::string argv0) {
 }
 
 std::string CommandLine::get_working_directory() {
+    return {};
+
     std::string workingDirectory = "";
     char        buff[40000];
     char*       cwd = GETCWD(buff, 40000);
diff --git a/src/nnue/network.h b/src/nnue/network.h
index cb43371..ca31203 100644
--- a/src/nnue/network.h
+++ b/src/nnue/network.h
@@ -69,6 +69,7 @@ class Network {
     Network& operator=(Network&& other)      = default;
 
     void load(const std::string& rootDirectory, std::string evalfilePath);
+    std::optional<std::string> load(std::istream&);
     bool save(const std::optional<std::string>& filename) const;
 
     std::size_t get_content_hash() const;
@@ -90,8 +91,6 @@ class Network {
     void initialize();
 
     bool                       save(std::ostream&, const std::string&, const std::string&) const;
-    std::optional<std::string> load(std::istream&);
-
     bool read_header(std::istream&, std::uint32_t*, std::string*) const;
     bool write_header(std::ostream&, std::uint32_t, const std::string&) const;
 
diff --git a/src/nnue/network.cpp b/src/nnue/network.cpp
index 767d936..b90db0b 100644
--- a/src/nnue/network.cpp
+++ b/src/nnue/network.cpp
@@ -319,7 +319,13 @@ std::optional<std::string> Network<Arch, Transformer>::load(std::istream& stream
     initialize();
     std::string description;
 
-    return read_parameters(stream, description) ? std::make_optional(description) : std::nullopt;
+    if (!read_parameters(stream, description))
+        return std::nullopt;
+
+    evalFile.current        = evalFile.defaultName;
+    evalFile.netDescription = description;
+
+    return description;
 }
 
 
diff --git a/src/syzygy/tbprobe.cpp b/src/syzygy/tbprobe.cpp
index 9fe6df9..53164d5 100644
--- a/src/syzygy/tbprobe.cpp
+++ b/src/syzygy/tbprobe.cpp
@@ -243,7 +243,7 @@ class TBFile: public std::ifstream {
 
         *mapping     = statbuf.st_size;
         *baseAddress = mmap(nullptr, statbuf.st_size, PROT_READ, MAP_SHARED, fd, 0);
-    #if defined(MADV_RANDOM)
+    #if defined(MADV_RANDOM) && !defined(__EMSCRIPTEN__)
         madvise(*baseAddress, statbuf.st_size, MADV_RANDOM);
     #endif
         ::close(fd);
diff --git a/src/timeman.h b/src/timeman.h
index 08e8da1..917d27e 100644
--- a/src/timeman.h
+++ b/src/timeman.h
@@ -48,7 +48,7 @@ class TimeManagement {
     TimePoint elapsed(FUNC nodes) const {
         return useNodesTime ? TimePoint(nodes()) : elapsed_time();
     }
-    TimePoint elapsed_time() const { return now() - startTime; };
+    TimePoint elapsed_time() const { return std::max(now() - startTime, TimePoint(1)); };
 
     void clear();
     void advance_nodes_time(std::int64_t nodes);
diff --git a/src/uci.cpp b/src/uci.cpp
index 9270076..7da00c7 100644
--- a/src/uci.cpp
+++ b/src/uci.cpp
@@ -39,6 +39,8 @@
 #include "types.h"
 #include "ucioption.h"
 
+std::string js_getline();
+
 namespace Stockfish {
 
 constexpr auto BenchmarkCommand = "speedtest";
@@ -93,9 +95,7 @@ void UCIEngine::loop() {
 
     do
     {
-        if (cli.argc == 1
-            && !getline(std::cin, cmd))  // Wait for an input or an end-of-file (EOF) indication
-            cmd = "quit";
+        cmd = js_getline();
 
         std::istringstream is(cmd);
 
diff --git a/src/uci.h b/src/uci.h
index ebc04fc..5350eb2 100644
--- a/src/uci.h
+++ b/src/uci.h
@@ -54,8 +54,9 @@ class UCIEngine {
 
     auto& engine_options() { return engine.get_options(); }
 
-   private:
     Engine      engine;
+
+   private:
     CommandLine cli;
 
     static void print_info_string(std::string_view str);
